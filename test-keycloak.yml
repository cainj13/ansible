- name: test my new module
  connection: local
  hosts: localhost
  tasks:
  - name: run the new module
    realms:
      name: master
    register: testout
  - name: dump test output
    debug:
      msg: '{{ testout }}'
